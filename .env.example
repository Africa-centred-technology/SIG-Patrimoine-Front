# URL de l'application « Espaces Verts » (greensig-front) embarquée.
# En dev : son serveur Vite tourne sur le port 3000 (voir apps/espaces-verts).
# En prod : servir son build et pointer cette variable vers ce chemin.
VITE_EV_APP_URL=http://localhost:3000
